# Запрашиваем путь к файлу
path = input("Введите путь к файлу: ")

# Разбиваем строку по символу '\'
# В Python обратный слэш экранируется, поэтому пишем '\\'
parts = path.split('\\')

# Выводим каждую часть на отдельной строке
for part in parts:
    print(part)
